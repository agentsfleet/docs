---
title: Run and control a fleet
description: Inspect, message, stop, resume, kill, and delete a fleet.
type: how-to
audience: user
verified: 2026-09-19
executable: false
---

# Run and control a fleet

## What this does

These commands show fleet activity and change lifecycle state. Every command uses the active workspace.

## Before you begin

Install a fleet. Copy its identifier from `agentsfleet list`.

## Steps

1. List fleet identifiers.

   ```bash
   agentsfleet list
   ```

   ```text
   <varies>
   ```

2. View recent activity.

   `<FLEET_ID>` comes from the list output.

   ```bash
   agentsfleet logs <FLEET_ID>
   ```

   ```text
   <varies>
   ```

3. Send a manual message.

   ```bash
   agentsfleet steer <FLEET_ID> "check current health"
   ```

   ```text
   <varies>
   ```

   A fleet whose `TRIGGER.md` declares `repository_access: write` opens an
   approval gate on every run. The reply arrives once you decide the gate with
   [`agentsfleet approvals`](/fleets/approvals).

   In the dashboard chat, a reply appears as it arrives. You can send another
   message while a run is active; the new event waits its turn and keeps its
   own place in the conversation. Reasoning and tool activity are shown
   separately from the final reply.

4. Stop new work while keeping the fleet resumable.

   ```bash
   agentsfleet stop <FLEET_ID>
   ```

   ```text
   ✓ <varies> stopped.
   ```

5. Resume the fleet.

   ```bash
   agentsfleet resume <FLEET_ID>
   ```

   ```text
   ✓ <varies> resumed.
   ```

## Verify it works

```bash
agentsfleet status
```

```text
Fleets
Name: <varies>
Status: active
Events: <varies>
Budget: <varies>
Waiting: <varies>
```

`Waiting` counts the approvals the fleet is parked on. When any fleet is
waiting, the client adds the line below.

```text
Some fleets are waiting on approval. Review with: agentsfleet approvals list
```

## Common problems

`UZ-AGT-009` means the fleet does not exist in the active workspace. Check the workspace and fleet identifier.

`UZ-AGT-012` means the fleet cannot accept a message in its current state. Resolve the cause or resume a stopped fleet.

A run that produces no reply may be parked at an approval gate. Run `agentsfleet approvals list` and decide the gate it names.

If a run fails while replying, open its event details in the dashboard and
click **Copy diagnostic**. The copy includes the event identifier and the
recorded failure cause. The chat keeps the failure message short.

Before a run starts, the runner downloads the fleet's bundle, making up to 3
attempts within 5 seconds when the connection drops. If every attempt fails,
the chat reports that the runner refused the run before the fleet started.
Your message stays in the conversation, so send it again.

A lifecycle command sends a write again only when the connection was refused, the name did not resolve, or the server declined it. A reset after sending or a timeout surfaces the error instead, because the server may have processed the write. Check `agentsfleet doctor`, then run the command again.

## Remove or undo

Stopping is reversible with `resume`. Killing is permanent but keeps the fleet records until deletion.

Deleting removes all fleet records and cannot be undone. The client has no dry-run option for either command.

```bash
agentsfleet kill <FLEET_ID>
```

```text
✓ <varies> killed.
```

```bash
agentsfleet delete <FLEET_ID>
```

```text
✓ <varies> deleted.
```

## Related pages

- [Fleet overview](/fleets/overview)
- [Approve a waiting run](/fleets/approvals)
- [Troubleshooting](/fleets/troubleshooting)
- [Command index](/cli/agentsfleet)
